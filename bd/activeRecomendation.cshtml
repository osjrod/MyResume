@{
    
    if (IsPost)
    {
        var id = Request["id"];

        var query = "update recommendations set active = 1 where id = "+id+";";

        var db = Database.Open("data");
        db.Execute(query);

    }

    Response.Redirect("/Admin");

}
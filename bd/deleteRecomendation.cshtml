@{
    if (IsPost)
    {
        var id = Request["id"];

        var query = "delete from recommendations where id = " + id + ";";

        var db = Database.Open("data");
        db.Execute(query);

    }

    Response.Redirect("/Admin");
}